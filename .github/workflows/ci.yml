name: ci

on:
  push:
  pull_request:

jobs:
  smoke:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Python version
        run: python3 --version

      - name: Smoke public workflow
        run: bash scripts/smoke-public-workflow.sh
