# Indexing Rule

`~/.tall-talents/index.md` is derived from talent package entrypoints.

Include only talents where `status: active`.

Sort entries by slug ascending.

Entry format:
- ``slug`` — summary Path: `talents/<slug>/TALENT.md`

Do not include changelog summaries in the index. Historical review belongs in package changelogs and self-iteration reports.
