# Archive Policy

Do not delete historical talents by default.

To retire a talent:
1. Set `status: archived` in front matter.
2. Optionally move file to `~/.tall-talents/archive/` when no longer needed in active tree.
3. Rebuild index (archived talents must not appear).

Draft talents may exist during authoring but should not appear in index.
